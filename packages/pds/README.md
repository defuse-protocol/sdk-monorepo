# # @defuse-protocol/pds

An SDK to facilitate Passive Deposit Service (PDS) address generation.

## Run tests

Before running tests you need to compile this binary into the `/src` folder as `near-pds-poc`
https://github.com/substance-labs/rust-pds-poc
