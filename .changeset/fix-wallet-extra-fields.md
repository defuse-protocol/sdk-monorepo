---
"@defuse-protocol/contract-types": patch
---

Allow extra fields in external wallet input schemas (TON, NEP-413, EVM) during runtime validation. Wallets can include fields outside the Defuse schema without failing validation.
